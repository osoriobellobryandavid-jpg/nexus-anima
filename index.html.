```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexus Anima Central</title>
    <link rel="manifest" href="manifest.json">
    <style>
        body { background: #000; color: #0f0; font-family: monospace; padding: 20px; }
        button { display: block; width: 100%; padding: 15px; margin: 10px 0; background: #111; border: 1px solid #333; color: #0f0; }
    </style>
</head>
<body>
    <h1>Nexus Anima</h1>
    <button onclick="alert('Módulo de música activado')">🎵 Música</button>
    <button onclick="alert('Módulo de video activado')">🎬 Video</button>
</body>
</html>
```
